const multiplicarNumero = (numero1: number, numero2: number) : number => numero1 * numero2

console.log(multiplicarNumero(2, 5))

function dizOla(nome: string): string{
    return `Olá ${nome}`
}

console.log(dizOla("Angelo"))